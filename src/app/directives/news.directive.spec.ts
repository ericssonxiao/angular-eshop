import { NewsDirective } from './news.directive';

describe('NewsDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsDirective();
    expect(directive).toBeTruthy();
  });
});
